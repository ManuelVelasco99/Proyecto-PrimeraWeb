<div class="container">
    <div class="row">
        <div class="col">
            <table class="table table-hover text-center">
                <thead>
                    <th>ID</th>
                    <th>Descripcion</th>
                    <th></th>
                    <th></th>
                </thead>
                <tbody>
                    {{#each categorias}}
                    <tr>
                        <td>{{idCategoria}}</td>
                        <td>{{descripcion}}</td>
                        <th>
                            <a href="/users/single/{{id}}" class="btn btn-link">Editar</a>
                        </th>
                        <td>
                            <a href="/users/single/{{id}}" class="btn btn-link">Eliminar</a>
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
    </div>
</div>