<section class="container mt-3 mb-5" name="login">
            <h2 class="text-center">Login</h2>
            <div class="row justify-content-center mt-3">
                <div class="col-6">
                    <form action="/login" method="POST">
                        <div class="form-group">
                            <label for="nombreUsuario">Usuario <span class="text-danger">*</span></label>
                            <input 
                                type="text" 
                                name="nombreUsuario" 
                                id="nombreUsuario"
                                class="form-control"
                                required                           
                            />
                        </div>

                        <div class="form-group">
                            <label for="password">Contraseña <span class="text-danger">*</span></label>
                            <input 
                                type="password" 
                                name="password" 
                                id="password"
                                required
                                class="form-control"
                            />
                        </div>
                        <div class="text-center">
                        <button type="sumbit" class="btn btn-primary btn-lg mt-2">Ingresar</button>
                        </div>

                    </form>
                    <div class="mt-5 text-danger block text-center">
                    <p>{{message}}</p>
                    </div>
                </div>
            </div>
        </section>